[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "review-loop"
version = "0.1.0"
description = "Automated implement → review → fix loop using the Cursor CLI"
requires-python = ">=3.9"

[tool.hatch.build.targets.wheel]
packages = ["src/review_loop"]
